import { HighlightRedDirective } from './highlight-red.directive';

describe('HighlightRedDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightRedDirective();
    expect(directive).toBeTruthy();
  });
});
